import React from "react";

function EditDescription() {
  return;
  <div>
    <h2>Update Description</h2>
    <input type="text" placeholder={product.description} value={} onChange={} />
  </div>;
}

export default EditDescription;
