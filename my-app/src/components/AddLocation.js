import React from "react";
import { Link } from "react-router-dom";

function AddLocation({ products }) {
    return (
        <div>
            {/* {products.map(product => (
                <Link key={product.id} to={``}
            )) */}
        </div>
    )
}

export default AddLocation;